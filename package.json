{
  "name": "sg-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "app",
    "app2"
  ],
  "scripts": {
    "build": "pnpm build:packages && pnpm build:apps",
    "build:packages": "cd packages/geometry && pnpm run build",
    "build:apps": "cd app && pnpm run build",
    "check": "pnpm lint && pnpm fmt:check",
    "format": "pnpm fmt:check",
    "format:fix": "pnpm fmt",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "test": "pnpm run test:packages && pnpm run test:apps",
    "test:packages": "cd packages/geometry && pnpm test",
    "test:apps": "cd app && pnpm test",
    "test:watch": "cd packages/geometry && pnpm run test:watch",
    "test:coverage": "cd packages/geometry && pnpm run coverage",
    "type-check": "cd packages/geometry && pnpm run type-check",
    "dev": "cd app && pnpm run dev",
    "dev:app2": "cd app2 && pnpm run dev",
    "prepare": "pnpm run build",
    "clean": "rm -rf **/node_modules **/dist **/coverage || true"
  },
  "devDependencies": {
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0"
  }
}
